<h2>Welcome to Sali.sh!</h2>
<h3><%= t 'devise.invitations.edit.header' %></h3>
<div class="formcenter">
<h2>Welcome to Sali.sh!</h2>
<%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => { :method => :put } do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :invitation_token %>

  <div><%= f.label :first_name %><br />
  <%= f.text_field :first_name %></div>

  <div><%= f.label :last_name %><br />
  <%= f.text_field :last_name %></div>

  <p><%= f.label :password %><br />
  <%= f.password_field :password %></p>

  <p><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation %></p>

  <div><%= f.label :image %> <i>(Profile Picture)</i><br />
  <%= f.file_field :image %></div>

  <p><%= f.submit t("devise.invitations.edit.submit_button"), class: "btn btn-large btn-primary"  %></p>
<% end %>
</div>