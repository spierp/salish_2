<header class="navbar navbar-fixed-top">
	<div class="navbar-inner">	
		<div class="container-fluid">
			<%= link_to image_tag("salish.png"), root_path, id: "logo"%>
						<% if signed_in? %>						
				<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
					<% if current_user.image? %>
					<%= image_tag current_user.image_url(:tiny).to_s, :class => "usericon" %>
					<% else %>
					<%= image_tag '/assets/blank_user.png'%>
					<% end %>
				</a>							
				<div class="nav-collapse">
					
					<ul class="nav pull-right">


							<li><%= link_to "Invite", users_path %></li>
							<li class="divider-vertical"></li>
							
							<li><%= link_to "Tribes", tribes_path %></li>
									
							<li class="divider-vertical"></li>
							<li id="fat-menu" class="dropdown">
								<a href="#" class="dropdown-toggle" data-toggle="dropdown">
									<% if current_user.image? %>
									<row><%= image_tag current_user.image_url(:tiny).to_s, :class => "usericon" %> <%= current_user.first_name %> </row>
									<% else %>
									<row><%= image_tag '/assets/blank_user.png' %> <%= current_user.first_name %> </row>
									<% end %>
									<b class="caret"></b>	
								</a>
								<ul class="dropdown-menu">
									<li><%= link_to '<i class="icon-pencil"></i> Posts'.html_safe, posts_path %></li>
									<li><%= link_to '<i class="icon-user"></i> My Settings'.html_safe, edit_user_registration_path %></li>				
									<li class="divider"></li>
									<li><%= link_to '<i class="icon-off"></i> Sign out'.html_safe, destroy_user_session_path, method: :delete %></li>	
								</ul>
								<li><div class="navprofilepic"></div></li>
						<% else %>
											<ul class="nav pull-right">
						<li><%= link_to "Sign in", 	new_user_session_path %></li>
						<% end %>

				</ul>
			</div>
		</div>	
	</div>
</header>

